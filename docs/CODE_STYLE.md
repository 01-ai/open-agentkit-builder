# 代码风格指南

## 自动格式化配置

本项目已配置完整的自动格式化工具链，确保代码风格统一。

### 🔧 配置文件

| 文件                    | 用途                           |
| ----------------------- | ------------------------------ |
| `.prettierrc`           | Prettier 格式化规则            |
| `.prettierignore`       | Prettier 忽略文件              |
| `.editorconfig`         | 编辑器通用配置                 |
| `.vscode/settings.json` | Cursor/VSCode 编辑器设置       |
| `eslint.config.mjs`     | ESLint 配置（已集成 Prettier） |

### 💾 保存时自动格式化

项目已配置 `.vscode/settings.json`，在 Cursor 或 VSCode 中保存文件时会自动：

1. ✅ 使用 Prettier 格式化代码
2. ✅ 修复 ESLint 可自动修复的问题
3. ✅ 自动整理 import 语句
4. ✅ 删除尾随空格
5. ✅ 在文件末尾添加换行符

### 📝 格式化规则

```json
{
  "semi": false, // 不使用分号
  "trailingComma": "es5", // ES5 兼容的尾随逗号
  "singleQuote": true, // 使用单引号
  "printWidth": 80, // 每行最多 80 字符
  "tabWidth": 2, // 缩进 2 个空格
  "useTabs": false, // 使用空格而非 Tab
  "arrowParens": "always", // 箭头函数总是使用括号
  "endOfLine": "lf", // 使用 LF 换行符
  "bracketSpacing": true, // 对象字面量使用空格
  "jsxSingleQuote": false, // JSX 使用双引号
  "proseWrap": "preserve" // Markdown 保持原始换行
}
```

### 🚀 命令行格式化

#### 格式化所有文件

```bash
pnpm format
```

#### 检查格式（不修改文件）

```bash
pnpm format:check
```

#### 修复 ESLint 错误

```bash
pnpm lint:fix
```

#### 只检查 ESLint

```bash
pnpm lint
```

### 📋 提交前检查

建议在提交代码前运行：

```bash
# 格式化代码
pnpm format

# 检查 ESLint
pnpm lint

# 构建项目（确保没有类型错误）
pnpm build
```

### 🔍 文件类型覆盖

自动格式化支持以下文件类型：

- JavaScript (`.js`, `.jsx`)
- TypeScript (`.ts`, `.tsx`)
- JSON (`.json`, `.jsonc`)
- CSS/SCSS (`.css`, `.scss`)
- Markdown (`.md`)

### 🛠️ Cursor/VSCode 推荐插件

确保安装以下插件以获得最佳体验：

1. **Prettier - Code formatter** (`esbenp.prettier-vscode`)
2. **ESLint** (`dbaeumer.vscode-eslint`)
3. **EditorConfig** (`editorconfig.editorconfig`)

### ⚙️ 自定义配置

如果需要调整格式化规则，编辑 `.prettierrc` 文件：

```json
{
  // 根据团队偏好修改
  "printWidth": 100, // 例如：增加到 100 字符
  "singleQuote": false // 例如：改用双引号
}
```

修改后运行 `pnpm format` 重新格式化所有文件。

### 🚫 忽略特定文件

如需忽略特定文件或目录，编辑 `.prettierignore`：

```
# 添加要忽略的路径
build/
coverage/
*.min.js
```

### 🎯 代码风格规范

#### TypeScript

- 使用严格模式
- 优先使用函数式组件
- 使用 ES6+ 语法
- 明确指定类型（避免 `any`）

#### React

- 组件使用 PascalCase 命名
- Props 使用 camelCase 命名
- 事件处理函数使用 `handle` 前缀（如 `handleClick`）
- 自定义 Hook 使用 `use` 前缀（如 `useWorkflow`）

#### 文件命名

- 组件文件：`ComponentName.tsx`
- 工具函数：`utility-name.ts`
- 类型定义：`type-name.ts`
- 页面路由：`page.tsx`（App Router 约定）

#### Import 顺序

自动按以下顺序整理：

1. React 相关
2. 第三方库
3. 项目内部模块（`@/`）
4. 相对路径导入
5. 样式文件

---

_配置日期：2025-10-11_
